{
  "_id": "{{@ system @}}",
  "operations": {
    "boards-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "url": "members/me/boards"
    }
  },
  "type": "system:rest",
  "url_pattern": "https://api.trello.com/1/%s?token={{@ token @}}&key=$SECRET(api_key)",
  "verify_ssl": true
}
